<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
        <a class="navbar-brand" href="@Url.Action("Index", "home")">A\\\</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#nav-bar-buttons" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="nav-bar-buttons">
            <ul class="navbar-nav me-auto">
                @using (Html.BeginForm("GetByTag", "Search"))
                {
                    <li class="nav-item">
                        <input type="hidden" value="movies250" name="tag" />
                        <input type="hidden" value="true" name="newRequest" />
                        <button class="btn nav-link" type="submit">
                            Top 250 Imdb Movies
                        </button>
                    </li>
                }
                @using (Html.BeginForm("GetByTag", "Search"))
                {
                    <li class="nav-item">
                        <input type="hidden" value="tvs250" name="tag" />
                        <input type="hidden" value="true" name="newRequest" />
                        <button class=" btn nav-link">
                            Top 250 Imdb TV Series
                        </button>
                    </li>
                }
                <li class="nav-item">
                    <form action="@Url.Action("GetByTag", "Search")">
                        <input type="hidden" value="liked" name="tag" />
                        <input type="hidden" value="true" name="newRequest" />
                        <button class="btn nav-link">Most Liked</button>
                    </form>
                </li>
                <li class="nav-item">
                    <form action="@Url.Action("GetByTag", "Search")">
                        <input type="hidden" value="watched" name="tag" />
                        <input type="hidden" value="true" name="newRequest" />
                        <button class="btn nav-link">Most Watched</button>
                    </form>
                </li>
                <li class="nav-item">
                    <button class="btn nav-link" data-bs-toggle="modal" data-bs-target="#search" href="">Search</button>
                </li>
            </ul>
            <hr />
            <ul class="navbar-nav ms-md-auto">
                <li class="nav-item">
                    <form action="@Url.Action("About", "Home")">
                        <button class="btn nav-link">About</button>
                    </form>
                </li>
                @Html.Partial("_LoginPartial")
            </ul>
        </div>
    </div>
</nav>
